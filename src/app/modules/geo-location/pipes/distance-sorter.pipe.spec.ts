import { DistanceSorterPipe } from './distance-sorter.pipe';

describe('DistanceSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
